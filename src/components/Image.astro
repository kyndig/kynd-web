---
import { Picture, type LocalImageProps } from 'astro:assets';

export type Props = LocalImageProps;

const { src, ...attrs } = Astro.props;
---

<Picture src={src} formats={['avif', 'webp']} {...attrs} />
