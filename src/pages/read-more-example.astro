---
import Base from '@/layouts/Base.astro';
import PageSection from '@/components/PageSection.astro';
import Prose from '@/components/Prose.astro';
import Link from '@/components/Link.astro';
import ReadMoreSec from '@/components/ReadMoreSec.astro';
---

<Base title="ReadMore Component Examples">
  <PageSection
    heading="Link Component Examples"
    subheading="Demonstrating the new Link component"
  >
    <Prose>
      This is an example of how the ReadMore component can be used. The component consists of a
      circular icon container with an arrow icon and a label. It supports multiple variants for
      different color schemes and use cases.
    </Prose>

    <h3>All Link Variants</h3>

    <div style="display: flex; flex-direction: column; gap: 2rem; margin: 2rem 0;">
      <div>
        <h4>Light Primary (default)</h4>
        <Link href="/" variant="light-primary" />
      </div>

      <div>
        <h4>Light Secondary</h4>
        <Link href="/" variant="light-secondary" />
      </div>

      <div>
        <h4>Dark Primary</h4>
        <Link href="/" variant="dark-primary" />
      </div>

      <div>
        <h4>Dark Secondary</h4>
        <Link href="/" variant="dark-secondary" />
      </div>

      <div>
        <h4>Green Primary</h4>
        <Link href="/" variant="green-primary" />
      </div>

      <div>
        <h4>Green Secondary</h4>
        <Link href="/" variant="green-secondary" />
      </div>
    </div>
  </PageSection>

  <PageSection
    heading="ReadMoreSec Component Examples"
    subheading="Demonstrating the new ReadMoreSec component with three variants"
  >
    <Prose>
      The ReadMoreSec component is a simpler alternative to the ReadMore component. It features a
      clean design with just text and a small arrow icon, available in three color variants: green,
      white, and dark.
    </Prose>

    <h3>All ReadMoreSec Variants</h3>

    <div style="display: flex; flex-direction: column; gap: 2rem; margin: 2rem 0;">
      <div>
        <h4>Green Variant (default)</h4>
        <ReadMoreSec href="/" />
      </div>

      <div>
        <h4>White Variant</h4>
        <ReadMoreSec href="/" variant="white" />
      </div>

      <div>
        <h4>Dark Variant</h4>
        <ReadMoreSec href="/" variant="dark" />
      </div>
    </div>

    <h3>ReadMoreSec on Different Backgrounds</h3>

    <div style="display: flex; flex-direction: column; gap: 2rem; margin: 2rem 0;">
      <div style="background: var(--color-dark-blue); padding: 2rem; border-radius: 8px;">
        <h4 style="color: white;">Dark Background - White Variant</h4>
        <ReadMoreSec href="/" variant="white" />
      </div>

      <div style="background: var(--color-light-green); padding: 2rem; border-radius: 8px;">
        <h4 style="color: var(--color-dark-blue);">Green Background - Dark Variant</h4>
        <ReadMoreSec href="/" variant="dark" />
      </div>

      <div style="background: var(--color-light-gray); padding: 2rem; border-radius: 8px;">
        <h4 style="color: var(--color-dark-blue);">Light Gray Background - Green Variant</h4>
        <ReadMoreSec href="/" variant="green" />
      </div>
    </div>
  </PageSection>

  <PageSection
    heading="Link within Prose Component"
    subheading="Examples of different variants in Prose components"
  >
    <div style="margin: 2rem 0;">
      <h4>Prose with Light Primary Link</h4>
      <Prose link={{ href: '/om-oss', variant: 'light-primary' }}>
        This Prose component automatically includes a Link component at the bottom because we
        passed the link prop with an href and variant. The component will appear below this text
        content, styled according to the specified variant.
      </Prose>
    </div>

    <div style="margin: 2rem 0;">
      <h4>Prose with Dark Primary Link</h4>
      <Prose link={{ href: '/folka', variant: 'dark-primary' }}>
        This example shows a Prose component with a dark primary Link variant. The black
        background with green arrow creates a nice contrast and follows the design system.
      </Prose>
    </div>

    <div style="margin: 2rem 0;">
      <h4>Prose with Green Primary Link</h4>
      <Prose link={{ href: '/prosjekter', variant: 'green-primary' }}>
        Here's a Prose component using the green primary variant. The green background with black
        arrow and text provides a vibrant, energetic appearance suitable for call-to-action
        sections.
      </Prose>
    </div>

    <Prose>
      This paragraph is outside of Prose, so it won't have a ReadMore component. Only the text
      content within Prose components can have ReadMore functionality.
    </Prose>
  </PageSection>
</Base>
